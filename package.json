{
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "name": "Aqcua_Galbaic_6",
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "prebuild": "npm run prebuild --workspaces --if-present",
    "pretest": "npm run prebuild --workspaces --if-present",
    "start": "npm start --workspaces --if-present",
    "test": "npm test --workspaces --if-present"
  },
  "type": "module",
  "workspaces": [
    "src/Aqcua_Galbaic_6_backend",
    "src/Aqcua_Galbaic_6_frontend"
  ],
  "dependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.10",
    "crypto": "^1.0.1",
    "express": "^4.19.2",
    "path": "^0.12.7",
    "querystring": "^0.2.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3",
    "zlib": "^1.0.5"
  }
}
